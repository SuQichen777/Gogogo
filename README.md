# Gogogo
A test Repo for Golang. I want to try different APIs using different tech-stacks.

### 本地引用
在进行本地引用前，首先要明白`Go`语言的强制模组架构：换言之，我们需要先进行`go mod init Gogogo`的初始化（`Gogogo`的使用只是因为它是目录文件夹名）。然后再进行：
```Go
import (
    "fmt"
    "Gogogo/一个package名"
)
```
的类似引用